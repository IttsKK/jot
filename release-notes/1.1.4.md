## Highlights

- Refined quick capture so slash commands only execute after a space, queue commands show their locked pill correctly, and meeting-style commands keep task parsing turned off while you type.
- Kept the quick capture input anchored in place while the panel grows or shrinks, so extra UI expands downward instead of shifting the typing row.
- Refreshed the website previews with a cleaner quick-capture mock and a dedicated Today checklist feature section.

## Improvements

- Added regression coverage for quick-capture command execution and chip visibility.
- Updated the landing page branding and feature visuals so the site better reflects the current app experience.
